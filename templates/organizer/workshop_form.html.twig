{% extends 'base.html.twig' %}{% block title %}{{ title }} — SkillSpot{% endblock %}{% block body %}<section class="auth-shell"><div class="auth-card"><p class="eyebrow">Espace organisateur</p><h1>{{ title }}</h1><p class="muted">Une description précise aide les participants à choisir en confiance.</p>{{ form_start(form) }}{{ form_widget(form) }}<button class="button">Enregistrer l’atelier</button>{{ form_end(form) }}</div></section>{% endblock %}
