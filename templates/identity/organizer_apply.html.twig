{% extends 'base.html.twig' %}
{% block title %}{{ 'organizer.application.title'|trans }}{% endblock %}
{% block body %}<section class="auth-shell"><div class="auth-card"><p class="eyebrow">{{ 'organizer.application.eyebrow'|trans }}</p><h1>{{ 'organizer.application.heading'|trans }}</h1><p class="muted">{{ 'organizer.application.intro'|trans }}</p>{{ form_start(form) }}{{ form_widget(form) }}<button class="button">{{ 'organizer.application.submit'|trans }}</button>{{ form_end(form) }}</div></section>{% endblock %}
