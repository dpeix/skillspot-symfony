{% extends 'base.html.twig' %}
{% block title %}{{ 'identity.reset.new_title'|trans }}{% endblock %}
{% block body %}<section class="auth-shell"><div class="auth-card"><p class="eyebrow">{{ 'identity.reset.new_eyebrow'|trans }}</p><h1>{{ 'identity.reset.new_heading'|trans }}</h1>{{ form_start(form) }}{{ form_widget(form) }}<button class="button">{{ 'common.save'|trans }}</button>{{ form_end(form) }}</div></section>{% endblock %}
